# SP3 Backend Implementation Plan (full-deep)

> **For agentic workers:** thực thi inline (executing-plans), 1 việc/commit, nhánh `feat/sp3-backend`.
> **STATUS (2026-07-08): active — W1+W2 XONG trên nhánh feat/sp3-backend (chưa merge; deploy gom W7).** Spec đã duyệt: `docs/superpowers/specs/2026-07-08-sp3-backend-design.md`. DoD = bảng 9 tiêu chí trong spec.
>
> **W1 (ruff) XONG:** pyproject.toml [tool.ruff] (E/F/W/ASYNC, ignore style cố ý) → ruff 773→**80**. Bắt+vá **bug runtime THẬT** (auth.py delete_trusted_device thiếu import check_rate → NameError; F821 ruff phát hiện) + regression test. Safe-fix 276 lỗi. Gate **R20.1** hard-ratchet baseline 80 (check_ruff.py, định vị ruff robust + graceful-skip). requirements-dev.txt.
> **W2 (async) XONG:** 4 nit ASYNC → noqa; gate **R20.2** hard-ratchet baseline 0.
> **W4-nền XONG:** `.coveragerc` (omit tests → số thật 51%) + `check_coverage.py` gate **R20.4** (đọc coverage.json, so `coverage-thresholds.json` ratchet nâng dần → agent 60/core 80; vắng file → skip). TDD. **3 pending-check R20.1+R20.2+R20.4 giờ ACTIVE + gate hạ tầng SP3 XONG.**
> **W6.1 (comment soft-delete) XONG:** migration 068 comments.deleted_at + partial index; delete_comment 2 DELETE→UPDATE deleted_at (reply con recoverable, hết mất-UGC); lọc deleted_at IS NULL ~9 site (display/count/profile/action-gates); test cấu-trúc (UGC PG-only → verify functional lúc deploy W7 --migrate). **2 bug thật của grounding đều đã vá (F821 + comment UGC-loss).**
> **Còn (đợt PAYDOWN — nặng, nhiều session/cần PG):** W3 complexity refactor 250→170 · W4.3/4.4 raise database→80/agent→60 + **W5 core-80% +3.464 stmt test (khổng lồ)** — cần pytest --cov nặng · W6.2 error-shape 36 site (cần tách _error_response ra module chung + plumb request) · W6.3 response_model · W7 deploy 1 lần (--migrate cho 068). Chạy với runway mới cho memory thoáng.

**Bất biến:** B3 test-trước-refactor vùng mù · B2 additive · B4 một-schema-một-test · B5 commit nhỏ · B1 backup trước data-op · KHÔNG one-shot ruff --fix · ratchet nợ chỉ giảm · deploy gom cuối.

**Môi trường:** ruff ở `C:/Python314/Scripts/ruff` (không qua `python -m ruff`); pytest-cov đã cài; 7 fail-đã-biết (6 PG-index + test_seo) + nhóm test_gap_fixes cần PG.

## W1 · Ruff config + safe-fix + F821
- [ ] **T1.1** Tạo `pyproject.toml` `[tool.ruff]`: `target-version`, `line-length` (khớp repo), `[tool.ruff.lint] select = ["E","F","W","I"]`, `per-file-ignores` (E402 cho file có sys.path hack — liệt kê sau khi grep), `exclude = ["web-nuxt",".nuxt"]`. Thêm `ruff` vào `requirements-dev.txt` (tạo nếu chưa có). Commit.
- [ ] **T1.2** Soi F821 (undefined-name): `ruff check agent/ scripts/ --select F821`. Đọc từng chỗ; nếu code-path thật → viết test tái hiện (B3) rồi sửa; nếu false (import cond) → sửa/ignore có lý do. Commit.
- [ ] **T1.3** Autofix lô 1 — F541 (63): `ruff check --select F541 --fix agent/ scripts/`. `pytest -q` đối chiếu fail-đã-biết. Commit.
- [ ] **T1.4** Autofix lô 2 — E401 (42): `--select E401 --fix`. pytest. Commit.
- [ ] **T1.5** Autofix lô 3 — F401 (167 safe): `--select F401 --fix`. Kiểm re-export `__init__` không mất. pytest. Commit.
- [ ] **T1.6** `scripts/checks/check_ruff.py` (hard-ratchet R20.1): chạy ruff `--select E,F,W,I` (nhóm đã bật, E402/E701/E702 nằm ngoài select nên = giọng repo giữ nguyên), đếm, so baseline. TDD fixture. Đăng ký run_hard + hook (đường ruff tường minh). Baseline = số đo sau safe-fix. Commit.

## W2 · Async lock
- [ ] **T2.1** Fix 3 ASYNC240: đưa `Path(__file__).resolve().parent` ra module-const `_MODULE_DIR` ở admin.py/social.py. 1 ASYNC110 server.py:858 → `# noqa: ASYNC110` + comment lý do (shutdown-drain poll hợp lệ). `ruff --select ASYNC agent/` = 0. Commit.
- [ ] **T2.2** Thêm `--select ASYNC` vào check_ruff (hoặc module riêng R20.2, hard baseline 0). Test. Commit.

## W3 · Complexity paydown (250 → ≤170)
- [ ] **T3.1** `agent/seo.py build_entity_jsonld` (134) → tách `_jsonld_address/_geo/_offers/_rating/_faq` helper. Đo complexity giảm. Nếu vùng có test → chạy; nếu không → thêm smoke test JSON-LD trước (B3). Commit.
- [ ] **T3.2** `agent/public_api.py homepage_curated` (80) → tách per-section helper. Test homepage endpoint trước. Commit.
- [ ] **T3.3** `_build_messages` (45) + `_build_user_interest_profile` (36) + `admin.list_entities/entity_completeness` (33/34) → tách. Commit từng hàm.
- [ ] **T3.4** Quét nhóm ~84 hàm dải 13-15 (danh sách từ check_complexity): tách 1 guard/loop mỗi hàm đưa ≤12. Commit theo lô ~10 hàm. Đo R20.8 ≤170. Rebaseline (giảm) cùng commit.

## W4 · Coverage nền + gate R20.4
- [ ] **T4.1** Tạo `.coveragerc` (`[run] omit = agent/tests/*, */tests/*`). Thêm `pytest-cov` vào requirements-dev. Commit.
- [ ] **T4.2** `scripts/checks/check_coverage.py`: đọc `coverage.json` (sinh bởi `pytest --cov=agent --cov-report=json`), gate R20.4 — agent-total ≥ ngưỡng + core-4 mỗi module ≥ ngưỡng; ratchet (ngưỡng nâng dần, không tụt). TDD với coverage.json giả. KHÔNG vào hook staged (chậm) — chỉ pre_merge/CI. Baseline = số hiện tại (agent 51%, core thấp). Commit.
- [ ] **T4.3** database.py 78.5%→80% (+13 stmt): đọc `--cov-report=term-missing` cho database.py, viết test cho dòng thiếu rẻ nhất. Commit.
- [ ] **T4.4** agent/ 51%→60%: test module trắng dễ — crawler.py (0%), freshness.py (12%), site_settings.py (14%), itinerary_gen.py (10%), recommender.py (12%), notifications.py (19%). Commit theo module. Đo agent-total ≥60%.

## W5 · Core coverage 80% (nhiều batch)
- [ ] **T5.1** auth.py 25.6%→80% (+707): map dòng thiếu → test theo nhóm (login/register/token/2FA/session/rate-limit). Commit theo nhóm ~150 stmt. B3: test-trước, không refactor auth trong đợt này trừ khi cần.
- [ ] **T5.2** social.py 18.7%→80% (+1351): test theo nhóm (post CRUD/comment/like/follow/feed/notification/reputation). Nhiều batch. Commit theo nhóm.
- [ ] **T5.3** server.py chat-path 21%→80% (+1406): test endpoint + chat handler nhánh (mock LLM). Batch theo endpoint-cluster.
- [ ] **T5.4** Đo core-4 mỗi module ≥80%; rebaseline R20.4 ngưỡng core.

## W6 · API-hygiene bugs
- [ ] **T6.1** Comment soft-delete: (a) migration `agent/migrations/NNN_comments_deleted_at.sql` thêm `deleted_at TIMESTAMPTZ` + index `WHERE deleted_at IS NULL`; (b) sửa `social.py delete_comment` → UPDATE deleted_at, BỎ `DELETE FROM comments WHERE parent_id` (reply con giữ nguyên/ẩn theo cha); (c) lọc `deleted_at IS NULL` mọi query list-comment + count. B1 backup + B4 test (soft-delete giữ reply, list ẩn comment đã xóa). Commit.
- [ ] **T6.2** Error-shape: sửa 36 chỗ `JSONResponse(content={"error":...})` (public_api 19 + server 17) → dùng `_error_response(status, detail, request_id)` (server.py:940). Gate: check_error_shape (grep-based hard-ratchet, baseline 0 sau sửa) chặn `JSONResponse({"error"` mới. Test 1-2 endpoint trả `{detail,request_id}`. Commit theo file.
- [ ] **T6.3** response_model: chọn ~15-20 endpoint public_api trọng yếu (homepage, entity detail, search, list...) → định nghĩa Pydantic response schema + `response_model=`. Test schema validate. Commit theo cụm.

## W7 · Kết đợt + deploy
- [ ] **T7.1** pytest full (fail-đã-biết không tăng) + build (không đụng FE nhưng chạy để chắc) + `python scripts/scorecard.py` (backend-dim TĂNG, không dim tụt) + baseline_tool cập nhật cùng commit giải trình.
- [ ] **T7.2** Plan-result vào plan này + `pre_merge_check feat/sp3-backend` + merge main.
- [ ] **T7.3** Deploy 1 lần: `deploy.sh --backend --migrate` (ship agent/*.py + data.json + migration). Migration comments.deleted_at áp qua flow (deploy.sh --migrate hoặc áp tay `psql -f` + `ALTER TABLE comments OWNER TO vl360` nếu cần). Verify home/agent/ready/search/public-api = 200. Rollback sẵn (backups/pre-deploy-*).
- [ ] **T7.4** Memory update (project-chuan-hoa-program: SP3 done + số cuối) + project-worldclass-be-audit (điểm mới).

**Điều kiện dừng:** coverage module không lên 80% sau 2 batch → dừng mức đạt + ghi hồ sơ, KHÔNG test rỗng. Migration prod lỗi → restore. Test xanh-bỗng-đỏ khi refactor → DỪNG (B3).
