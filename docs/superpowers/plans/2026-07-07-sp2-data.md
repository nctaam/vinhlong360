# SP2 Data Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). 1 việc/commit, nhánh `feat/sp2-data`.
> **STATUS (2026-07-07): active — đang thực thi.** Spec: `docs/superpowers/specs/2026-07-07-sp2-data-design.md`. Quyết định chủ (2026-07-07): **BỎ HẲN điểm lân cận ngoài tỉnh** (định vị thuần Vĩnh Long).

**Goal:** 3 kho đồng nhất (prod = data.json = local) sau khi: xoá 3 entity ngoài tỉnh + 23 relationships, cắt chặng ngoài tỉnh trong 2 itinerary-entity, đẩy 54 entity local-only lên prod; bật gate R10.3b (per-type required, tái dùng entity_schemas) + R10.8 (RICH-source); shape export round-trip.

**Bất biến:** B1 backup MỚI cả 2 DB trước xoá; B7 mọi apply có --dry-run; R70.5.

## Task
- [x] **T1a** Build gói thay đổi `sp2_changes.json`: DELETE_IDS = [cho-noi-cai-be, tour-song-nuoc-cai-be-vinh-long, hu-tieu-ca-phe-cho-cai-be-quan-truoc-cong-cho (id đầy đủ tra runtime)] + relationships dính; TEXT_FIX 2 itinerary (cắt block Cái Bè/Định Yên, old→new full-value); LOCAL_FIX nha-tho-chinh-toa address; WHITELIST+ bao-tang-khmer (tên riêng).
- [x] **T1b** Backup mới: pg_dump prod + copy SQLite + backup_data.py.
- [x] **T1c** `scripts/sp2_reconcile.py` (--dry-run mặc định): xoá + text-fix trên prod PG; đẩy 54 local-only (đã LOCAL_FIX) lên prod (INSERT per-column, verify count); chạy thật sau dry-run sạch; restart vl-agent + health.
- [x] **T1d** Export prod (thực tế 1.766 — xem KẾT QUẢ) → data.json (shape chuẩn T2 trước) + local SQLite nhận toàn văn prod (replace có ALLOW cờ, backup đã có); verify 3 kho: cùng count + text đồng nhất (hash so sánh).
- [x] **T2** export_data.py: chuẩn shape data.json (drop 8 cột phẳng, giữ createdAt từ created_at) + round-trip test (TDD).
- [x] **T3** check_data_schema: + R10.3b per-type required (gọi entity_schemas.validate_attributes, hard-ratchet, baseline đo lúc bật) + R10.8 RICH-source (hard-ratchet baseline 0, dùng seo.is_index_worthy).
- [x] **T4** R10.9 sau xoá: chạy lại content_voice — kỳ vọng còn ~0-5 occ (itinerary đã cắt); còn lại rà tay/sửa; cập nhật baseline TRONG CÙNG COMMIT với giải trình.
- [x] **T5** Kết: pytest + build + scorecard (data-dim không tụt, kỳ vọng TĂNG) + plan-result + merge main + memory.

**Điều kiện dừng:** prod apply lỗi → restore pg_dump, báo người; itinerary sau cắt gãy văn → viết lại tối thiểu KHÔNG bịa fact mới.

## KẾT QUẢ THỰC THI (2026-07-07)

**Số cuối khác plan — có lý do:** plan dự 1.784 (1730 − 3 + 54 + 2 prod-only đã tính trước); review 3 tầng (38 agent, 54 entity) hạ xuống **đẩy 39** (20 PUSH + 19 FIX-đã-áp-26-field) và **giữ lại 15**: 8 id XOÁ local (rác kỹ thuật test-mutation-*/prov-1, bảng-tài-trợ-crawl trong summary nhà thờ, chùa Sà Lôn = Sóc Trăng, chùa Xvayton/chùa Rô = An Giang, coords TP.HCM) + 7 HOLD chờ biên tập. Kết quả: **prod = data.json = 1.766**, local = 1.773 (= 1.766 + 7 HOLD), relationships 12.185, text chung 3 kho khác = **0**.

**7 HOLD chờ biên tập (chỉ còn ở local, chưa lên prod):** ham-luong, chien-thang-giong-dua-giong-trom, chua-botum-rangsay, khu-bao-ton-lung-binh-hoa-tra-vinh, chom-chom-binh-hoa-phuoc, lang-nghe-tac-tuong-go-long-duc, thanh-loc (nhầm-thực-thể NSƯT Thành Lộc — cần viết lại từ nguồn). Xử lý ở SP6.

**Ratchet tự chặn 2 lần trong đợt — hoạt động đúng thiết kế:**
1. R50.2 439>430: 39 entity mới mang 10 filler "miền Tây" → sửa THẬT bằng đặc-thù (Ngãi Tứ, vàm Trà Ôn, kênh Măng Thít…) trên cả 3 kho.
2. R20.8 252>250: sp2_reconcile.py 2 hàm quá phức tạp → refactor tách hàm, không baseline-hoá.

**Gate mới bật:** R10.3b baseline 49 (nợ per-type required, trả dần) · R10.8 baseline 0 (439 RICH đủ source — khoá thành quả) · R10.9 28 → **0** (6 occ cuối là FE, thay bằng chiếu Cà Hom/chợ nổi Trà Ôn/cù lao Mây; sửa luôn claim 9.500 → hơn 12.000 mối quan hệ). Whitelist-ngoai-tinh.txt KHÔNG cần nữa (hết ca hợp lệ sau quyết định bỏ điểm ngoài tỉnh).

**Nợ mới ghi nhận (giải trình trong commit 1987cef):** R50.4 244→245 (banh-mi-sau-hoa-ben-tre 190/200 ký tự — không viết đệm, chờ SP6).

**Deploy note:** prod DB + vl-agent ĐÃ mang data mới (health 200); FE prod + web/data.json trên VPS vẫn bản cũ (scheduler data-sync trên VPS còn đọc data.json cũ 12.208 rels) — **lần deploy tới phải mang cả data.json + FE build mới**. Deploy public = chủ dự án quyết (§4).
