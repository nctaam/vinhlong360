# SP2 Data Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (inline). 1 việc/commit, nhánh `feat/sp2-data`.
> **STATUS (2026-07-07): active — đang thực thi.** Spec: `docs/superpowers/specs/2026-07-07-sp2-data-design.md`. Quyết định chủ (2026-07-07): **BỎ HẲN điểm lân cận ngoài tỉnh** (định vị thuần Vĩnh Long).

**Goal:** 3 kho đồng nhất (prod = data.json = local) sau khi: xoá 3 entity ngoài tỉnh + 23 relationships, cắt chặng ngoài tỉnh trong 2 itinerary-entity, đẩy 54 entity local-only lên prod; bật gate R10.3b (per-type required, tái dùng entity_schemas) + R10.8 (RICH-source); shape export round-trip.

**Bất biến:** B1 backup MỚI cả 2 DB trước xoá; B7 mọi apply có --dry-run; R70.5.

## Task
- [ ] **T1a** Build gói thay đổi `sp2_changes.json`: DELETE_IDS = [cho-noi-cai-be, tour-song-nuoc-cai-be-vinh-long, hu-tieu-ca-phe-cho-cai-be-quan-truoc-cong-cho (id đầy đủ tra runtime)] + relationships dính; TEXT_FIX 2 itinerary (cắt block Cái Bè/Định Yên, old→new full-value); LOCAL_FIX nha-tho-chinh-toa address; WHITELIST+ bao-tang-khmer (tên riêng).
- [ ] **T1b** Backup mới: pg_dump prod + copy SQLite + backup_data.py.
- [ ] **T1c** `scripts/sp2_reconcile.py` (--dry-run mặc định): xoá + text-fix trên prod PG; đẩy 54 local-only (đã LOCAL_FIX) lên prod (INSERT per-column, verify count); chạy thật sau dry-run sạch; restart vl-agent + health.
- [ ] **T1d** Export prod (1.781 = 1730 − 3 + 54) → data.json (shape chuẩn T2 trước) + local SQLite nhận toàn văn prod (replace có ALLOW cờ, backup đã có); verify 3 kho: cùng count + text đồng nhất (hash so sánh).
- [ ] **T2** export_data.py: chuẩn shape data.json (drop 8 cột phẳng, giữ createdAt từ created_at) + round-trip test (TDD).
- [ ] **T3** check_data_schema: + R10.3b per-type required (gọi entity_schemas.validate_attributes, hard-ratchet, baseline đo lúc bật) + R10.8 RICH-source (hard-ratchet baseline 0, dùng seo.is_index_worthy).
- [ ] **T4** R10.9 sau xoá: chạy lại content_voice — kỳ vọng còn ~0-5 occ (itinerary đã cắt); còn lại rà tay/sửa; cập nhật baseline TRONG CÙNG COMMIT với giải trình.
- [ ] **T5** Kết: pytest + build + scorecard (data-dim không tụt, kỳ vọng TĂNG) + plan-result + merge main + memory.

**Điều kiện dừng:** prod apply lỗi → restore pg_dump, báo người; itinerary sau cắt gãy văn → viết lại tối thiểu KHÔNG bịa fact mới.
