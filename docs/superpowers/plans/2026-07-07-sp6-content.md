# SP6 Content v2 Implementation Plan

> **For agentic workers:** thực thi inline (executing-plans), 1 việc/commit, nhánh `feat/sp6-content`.
> **STATUS (2026-07-07): active — đang thực thi.** Spec đã duyệt: `docs/superpowers/specs/2026-07-07-sp6-content-design.md` (v2 khắc khe). DoD = bảng 15 tiêu chí trong spec.

**Goal:** content về-0 các nợ máy-đo-được (filler/formula/superlative/desc==summary/R10.3b), thin còn lại là nợ-có-hồ-sơ, R10.8 siết URL-ngoài, mọi bài viết lại có nguồn đã fetch + verify 2 tầng.

**Bất biến:** B1 backup trước MỌI thao tác dữ liệu · R70.5 mọi apply có --dry-run · mỗi batch ≤50 entity/commit · prod ghi xong restart vl-agent + health 8360 · KHÔNG bịa fact (R10.5).

**Hạ tầng tái dùng (đã có từ SP2):** pattern áp-3-kho match-old (scratchpad fix_fillers.py: mode data/sqlite/pg) · sp2_sync.py verify (đồng nhất 3 kho) · flow VPS (scp + venv python + pg_dump backup).

## Task

- [ ] **T1 · W1 dọn nền**: backup_data.py → xoá 7 HOLD khỏi local SQLite (entities + entities_fts nếu có; đã kiểm 0 tham chiếu) → verify local = 1.766 = data.json, text khác 0 → commit.
- [ ] **T2 · W2 enum + auto-fill R10.3b = 0**: (a) TDD test OFFICE_KINDS có 4 mục mới (giao_thong/ngan_hang/vien_thong/cua_hang) + label "Loại cơ quan / tiện ích"; (b) sửa agent/entity_schemas.py; (c) script sp6_fill_required.py sinh 49 fix (21 suy-được có lọc bẫy: 'mộc mạc/trường hợp/quảng trường/ATM-tại-bệnh-viện'; 28 theo enum mới) — dry-run in bảng → execute áp local + data.json + prod; (d) verify check R10.3b = 0; commit.
- [ ] **T3 · W3 nối nguồn**: workflow match corpus→entity (21 hồ sơ + catalog 62 + evidence-matrix BáoVL + THVL urls + web-log MATCH top_url cho iw thiếu URL-ngoài) → additions [{id, url, title}] CHỈ THÊM không ghi đè; spot-check ≥20 mẫu; áp 3 kho; đo lại iw-thiếu-URL-ngoài (89 → kỳ vọng ≤30). Ghi chú kỹ thuật: KHÔNG di dời nhãn quy trình (curated/auto-learn) khỏi source — R10.8 v2 đo URL-ngoài trực tiếp nên nhãn tự bị loại khỏi "đạt", tránh migration rủi ro; nêu trong plan-result.
- [ ] **T4 · W4bis không-bỏ-qua-mù**: workflow per-entity — (a) 36 iw trống nguồn: web-research tìm nguồn (tìm được → nối + xếp vào nhóm A; không → fact-audit cắt claim không truy được / đề xuất noindex per-entity); (b) 83 accommodation R50.4: fetch web-log top_url, có fact thật → hàng chờ viết D, không → verdict no-source. Kết cục 119/119 ghi campaign-log.
- [ ] **T5a · Campaign nhóm A (~90 iw)**: pipeline per-entity: gather nguồn (fetch toàn văn URL + trích corpus) → writer (chỉ dùng fact từ gói nguồn; giọng playbook §4) → fact-lens (đối chiếu từng fact, loại fact không truy được) → voice-lens (rubric §8, <8/10 viết lại, tối đa 2 vòng) → batch fixes JSON → áp 3 kho theo batch ≤50 + commit/batch. Ngưỡng dừng 5% + log verdict.
- [ ] **T5b · Nhóm B (67 dải 120-129) + C (113 desc==summary)**: B thêm fact từ nguồn vượt ngưỡng 130 (không nguồn → verdict); C có nguồn → description riêng, không → description trống. Cùng pipeline verify như T5a.
- [ ] **T5c · Nhóm D (~209 cụm 407 có specialty)**: dệt specialty/must_order/địa chỉ trung thực; generic vẫn thử web-log trước khi chấp nhận ngắn. Đích R50.4 ≤90.
- [ ] **T5d · Nhóm E (sweep "miền Tây" còn lại toàn kho, sau A-D) + F (sweep "huyện/thị xã" đơn-vị-cũ)**: extract occurrences → agents viết lại per-occurrence (chỉ xoá/thay filler bằng đặc thù SẴN CÓ trong entity, không thêm fact mới) → diff-guard → áp 3 kho. Whitelist per-occurrence cho danh-từ-riêng + địa-văn-hoá (data + FE) vào `docs/standards/whitelist-mien-tay.txt` (+ file huyện nếu cần).
- [ ] **T6 · W5 gates (TDD như SP1)**: R50.2 đọc whitelist + nâng hard-ratchet · R50.3 check mới (^Tọa lạc | ^Nằm (ở|tại|trong|bên) | câu-đầu-chứa-" là một " | kết-sáo; quét summary + đệ quy attributes) hard-ratchet · R50.5 superlative-trơ soft-ratchet · R10.10 đơn-vị-HC-cũ soft-ratchet · R10.8 v2 URL-ngoài (sửa DataRichSourceCheck + test). Đo baseline thật: kỳ vọng R50.2=0, R50.3=0, R50.5=0, R10.10=0, R10.8≤10.
- [ ] **T7 · W6 kết đợt + re-audit**: re-audit mẫu ≥10% bài đã áp (agent thứ ba, từ nguồn gốc) → sạch mới đi tiếp; đồng bộ 3 kho cuối (verify text khác 0) + baseline --write cùng commit giải trình + pytest/build + scorecard (content-dim ≥80, không dim tụt) + plan-result (kèm campaign-log verdict + bảng DoD 15 mục) + pre_merge + merge main + memory.

**Điều kiện dừng:** theo spec (nguồn mâu thuẫn → giữ + ghi hồ sơ; >5% fact không nguồn → DỪNG sửa prompt; re-audit bắt fact bịa → DỪNG rà 100% batch; rubric trượt 2 vòng → giữ text cũ, KHÔNG hạ chuẩn).

## KẾT QUẢ THỰC THI (2026-07-07)

- **T1** xong: xoá 7 HOLD + 3 fixture-test lọt DB (prov-1, test-mutation-*) → 3 kho = 1.766. Backlog: sửa test_kb_curation dùng tmp DB.
- **T2** xong: OFFICE_KINDS +4 mục (TDD + FE contract), auto-fill 49/49 required per-type → **R10.3b = 0** (3 kho). Vá bug hook check_api_contract (cp1252 crash trên diff tiếng Việt).
- **T3** xong: nối 124 nguồn corpus/web-log vào 99 entity (verify 2 tầng loại 5 map nhầm). R50.2 field-aware (bỏ source titles) **429 → 389**. Refactor giữ complexity = 250.
- **T4/W4bis** xong (workflow 13 agent, 178 web-search/fetch): triage 83 accom + web-research 70 iw trống nguồn → **75 add-source / 28 has-local-facts / 33 no-source / 18 cut-or-noindex**. Áp 73 nguồn web sạch → **iw thiếu URL ngoài: 123 → 24**. 3 kho đồng nhất.

### 17 iw CLAIM KHÔNG KIỂM CHỨNG ĐƯỢC / SAI (W4bis) — ĐÃ XÓA (chủ quyết 2026-07-07: "xóa cả 17")
3 kho: 1766 → **1749** (−17 entity, −125 rels). Backup pre_sp6_del17 trước. Web-research đầy đủ vẫn không có nguồn đáng tin; nhiều ca giống HOLD (nghi bịa/sai địa bàn):
- **Sai địa bàn**: chua-khmer-lang-cat-ben-tre (Láng Cát thực ở Kiên Giang), cong-ty-tnhh-ca-cao-thanh-dat (Ca cao Thành Đạt ở Bà Rịa-Vũng Tàu), bun-mam-mien-tay-vinh-long (nguồn thực ở Cà Mau), lau-cua-dong (nguồn không nhắc Vĩnh Long), thanh-that-cao-dai-tra-vinh-tra-vinh (mốc 1927 = Tòa Thánh Tây Ninh).
- **Không xác chứng tồn tại/claim**: cu-lao-an-loc-ban-chat, di-tich-ao-ho-co-vinh-xuan + di-tich-co-vinh-xuan-tra-on ("ao hồ cổ Óc Eo" — thực là đình), kenh-tra-vinh-tra-cu-tra-vinh (kênh 42km), rach-ba-sach-tra-vinh, rach-chua-kenh-chua-ben-tre, kenh-cho-lach-ben-tre, cua-ben-gia-my-thanh-tra-vinh ("Bến Giá" = trại giam), dinh-trieu-quoc-dat-dinh-than-trieu-minh-cong, thanh-that-cao-dai-vinh-long.
- **Có thật nhưng chỉ nguồn SEO-blog**: lang-nghe-dong-thuyen-an-dinh, tuyen-xe-buyt-noi-tinh-tra-vinh.

**Đề xuất**: noindex 17 ca (an toàn, đảo được) + nhóm sai-địa-bàn/nghi-bịa ưu tiên xoá như HOLD. Chờ chủ quyết delete vs noindex.

### Còn lại
- **T5** campaign viết lại (~90 iw filler/formula + 67 dải 120-129 + 113 desc==summary + ~209 cụm 407 + 28 has-local-facts accom) — phần lớn nhất.
- **T6** gates R50.3/R50.5/R10.10 + R10.8-v2. **T7** kết đợt.
