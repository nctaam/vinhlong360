# Nhật ký ngoại lệ & SKIP — vinhlong360
> **STATUS (2026-07-07): active — file máy-ghi (COMMITTED, không gitignore).**

## Ngoại lệ rule đã ký (chủ dự án)
- 2026-07-07 · R30.5 season-ring 22px (hình học — đo thực nghiệm 11/12 tap sai khi nới 44px); month-grid = fallback a11y.
- 2026-07-07 · R40.4 hero Ken Burns + parallax homepage — signature, có prefers-reduced-motion.
- 2026-07-07 · R40.5 top-nav + hamburger là chuẩn điều hướng đã chọn (không bottom-nav).
- 2026-07-07 · R10.5, R20.6, R30.4, R50.6, R70.2, R70.5 — dạng checklist/quy-trình (không máy-đo trọn vẹn).
- 2026-07-10 · **R20.8 = 3 (server.py pipeline-monster còn lại) DEFER có-lý-do** (soft-ratchet). 99/102 offender đã ≤12. **_build_messages (cx45) ĐÃ REFACTOR** (2026-07-10, sau khi khôi phục chat-integration coverage): phân rã theo 7-pha-docstring thành `_gather_context_pieces` + `_resolve_base_prompt` + `_fold_experience_fewshot` (+`_fold_lazy_prompt` khử lặp experience/fewshot DRY) + `_assemble_manual_messages`, shell còn ~4. **Verify GOLDEN byte-identical 4 case** (subprocess capture, chuẩn-hoá dòng ngày volatile) — extract-verbatim. Chat-integration fixture ĐÃ SỬA (lifespan reset _draining + fixture server.get_client): test_chat_tools 14/14 + test_chat_smoke 22/22 pass (34 test khôi phục, 7 stale-drift sửa khớp hành-vi-thật). **7 hàm server.py ĐÃ REFACTOR (2026-07-10, test-first THỦ CÔNG, không grind mù như agent-fail trước):** _call_tool_impl (cx131→dict-dispatch `_TOOL_HANDLERS` + 18 per-tool helper + decompose _tool_search/ocop/accommodation) · gate_internal_endpoints (path-list→constants) · _hybrid_rerank_search (tách rerank/backfill) · _resolve_contextual_query (dicts→const + 3 helper) · mention_search (tách _mention_users/entities) · _health_detail (feature-dict comprehension) · **_run_agent (cx17→ tách `_prepare_pending_calls` parse+đếm tool-call + `_execute_pending_calls` parallel/serial exec; extract-VERBATIM giữ nguyên bất-nhất parallel-branch KHÔNG cập nhật empty_results_count vs serial-branch có; test unit `patch(server.call_tool)` KHÔNG cần LLM/KB/fixture — 5 test: parse/bad-json/limit/serial/parallel).** **Verify mỗi hàm: GOLDEN byte-identical/id-list/stable-content** (extract-verbatim) + structure-test move-not-delete (weather-CB wiring, gate `_GATED_*`, mention `_mention_users`) + full agent/tests 0-regression per commit. **3 hàm còn lại** = chat cx111, chat_stream 97, event_stream 62 — pipeline request-handler state-heavy = **essential-complexity** (sequential decision-flow request→response/stream, mutable state đan xen; ép ≤12 = helper-soup nuốt-context rủi ro), DEFER hợp lệ. (_build_messages đã tách được vì có ranh-giới-pha rõ theo docstring; 3 cái này KHÔNG.) (Bonus phát hiện: chat integration fixture `server.client` lỗi thời — server dùng `get_client()` + lifespan `_shutting_down` không reset lúc startup → cả suite test_chat_smoke/test_chat_tools SILENT-error/pollute khi chạy `-m integration`; deselect ở `pytest -q` nên đi qua. **Backlog §5** ROADMAP: sửa fixture patch `server.get_client` + reset shutdown-flag để phục hồi chat integration coverage — cần cả PG-env cho ~20 admin/UGC test. KHÔNG làm trong scope refactor này. Bug pre-existing knowledge.directory_search crash source=list — đã spawn_task.)

## Đính chính đo lường (CHƯA có quyết định chủ dự án — KHÔNG phải ngoại lệ đã ký)

- 2026-07-26 · **R20.8 baseline 3 → 14: sửa THƯỚC ĐO, không phải nợ mới.** `check_complexity.py` đọc source bằng `encoding="utf-8"`; BOM UTF-8 sống sót thành U+FEFF nên `ast.parse` ném `SyntaxError` và file bị `except SyntaxError: continue` nuốt im lặng. Hệ quả: **`agent/gpt55_quality_burst.py` (69KB, có BOM) chưa từng được R20.8/R20.3 quét**. Sau khi đổi sang `utf-8-sig`, lộ **11 hàm >12**: `relationship_targets` (27), `generate_heuristic_eval_cases` (24), `placeid_candidate_from_decision` (21), `validate_candidate_record`/`source_candidate_for_entity`/`audit_relationship_chunk` (19), `candidate_places_for_entity` (18), `build_manifest`/`audit_accuracy_chunk` (17), `heuristic_quality_status`/`enforce_apply_policy` (14). 3 hàm `server.py` cũ vẫn nguyên → 3+11=14.
  **Đính chính claim cũ:** dòng "99/102 offender đã ≤12" (2026-07-10) đo bằng thước hỏng; con số thật là 99/113. Nợ này **đã tồn tại suốt**, chỉ vô hình — baseline 14 là ghi nhận số đọc đúng, KHÔNG phải baseline-hoá vi phạm mới.
  **Cần chủ dự án quyết:** refactor 11 hàm này (giảm dần như đợt `server.py` 12→3) hay DEFER có-lý-do như 3 hàm pipeline `server.py`. Chưa ai ký DEFER cho 11 hàm này.
  Ratchet vẫn có răng: 14 là trần mới, mọi vi phạm PHÁT SINH THÊM vẫn bị chặn.

## Floor legitimate (baseline không-nên-về-0 — đã trả hết phần mechanical)
- 2026-07-10 · **R30.3 fe_colors floor = 306** (từ 1373; 309→306 sau audit adversarial độc lập 5-agent read-only). **Audit xác nhận floor 306/309 hợp lệ** (svg_attr 60 + dark_override 76 + other_floor 170 = 306) + tìm **3 tokenizable AN TOÀN đã áp** (verified render y hệt): data-quality.vue×2 (`var(--warning-bg, rgba(230,126,34,.08))` → `var(--warning-bg)` — fallback dead-code vì --warning-bg định nghĩa light+dark) · nhat-ky.vue:149 (`rgba(255,69,58,.15)` → `rgba(var(--red-rgb),.15)` — light khớp --red-rgb chính xác; dark bị `.dark .audit-method.delete` che nên không lệch). 306 CÒN LẠI: Đã trả: 482 false-positive token-rgba, 67 hex→token, 450 overlay trắng/đen→primitive, 16 systemGray→primitive, 158 dead hex-fallback. 309 CÒN LẠI = ~185 màu trong **SVG presentation-attribute** (fill=/stroke=/stop-color= của illustration Hero/Avatar/UserCover — SVG-attr KHÔNG resolve `var()`, phải literal) + ~124 **one-off/decorative** (#74abb5×52 màu trang trí, one-off khớp token .dark-override = cấm thay). Tokenize artwork = sai ngữ nghĩa; token cho one-off = token-bloat. Floor hợp lệ.
- 2026-07-10 · **R30.2 emoji floor = 626 grandfathered-legitimate.** Phân tích: 550 **string-context** (SEO meta / map label / select option trong `<script>` — emoji ĐÚNG chỗ, đổi = vỡ SEO/nhãn) + ~98 **decorative-editorial** (hero-glyph `aria-hidden` 🏛️🎋🏅, marker động 👤/📍 — thiết-kế cinematic-editorial CHỦ ĐÍCH) + phần functional ✕✓☰ rải rác admin nội-bộ (nhiều cái CSS `content:` không convert IconLine được). Soft-ratchet đã phục vụ đúng: chặn emoji functional MỚI. Ép giảm = degrade design + rủi-ro regression cho chỉ-số nội-bộ. Floor hợp lệ.
- 2026-07-10 · **R50.2 content-filler floor = 102** (từ 340, −238/70%; 104→102 sau audit adversarial độc lập). **Audit xác nhận 102/104 hợp lệ** (region_name 70 + climate_template 17 + evidenced_fact 12 + brand_name 3 = 102) + **2 removable_filler đã áp** (verified 0 mất fact 0 bịa): itinerary "Tuần trăng mật Miền Tây" summary+description bỏ tính từ sáo "hữu tình" ("sông nước hữu tình"→"sông nước"; check FILLERS bắt nguyên cụm nên clears, "sông nước" đơn KHÔNG phải pattern). Edit-record content_surgical +1 để sync DB lúc deploy. 3 đợt STRICT rephrase-only + RE-AUDIT ĐỘC LẬP (chống bịa, bài học SP6 32%): top-level 131 field + nested 92 occ + content-surgical 9. 104 CÒN LẠI = **legitimate**: "miền Tây Nam Bộ" (tên vùng đúng ~40), fact/claim vùng có evidence ("Thủ phủ cam sành miền Tây 10.389ha", "chợ nổi sầm uất nhất miền Tây"), khí-hậu-template ("Thời tiết miền Tây" ~17), nghệ thuật vùng (ĐCTT/tam đại danh tửu), shop brand-name. Ép giảm nữa = xoá tên-vùng-đúng / bịa / sửa tên-thương-hiệu. (Proper-noun "Bến xe Miền Tây" M-hoa không bị check đếm, đã giữ.)
- 2026-07-10 · **R50.3 formula floor = 8** (từ 39). 32 mở-bài công thức đã restructure STRICT (đảo chủ ngữ, bỏ "Nằm/Tọa lạc/là một"). 8 còn = edge/nested field ngoài extraction top-level — giá-trị-biên, defer.
- 2026-07-10 · **R50.7 superlative = 24 floor** (soft-ratchet). Flag "nổi tiếng/nhất vùng" KHÔNG kèm số. STRICT (§1.7 cấm bịa) ⇒ KHÔNG được THÊM evidence/số để "chữa"; chỉ có thể xoá superlative (mất thông tin thật) hoặc để nguyên. Nhiều cái là claim-vùng hợp lý ("chợ nổi sầm uất nhất", "vùng nem chua nổi tiếng"). Giảm đúng cách = content-creation có nguồn (backlog), không grind.
- 2026-07-10 · **R50.4 thin = 245 IRREDUCIBLE** (soft, report-only). Entity summary+description <200 ký tự (shop/quán nhỏ/listing). §1.7 + bài học SP6 (enrichment tự-do=32% bịa) ⇒ CẤM độn nội-dung để đạt ngưỡng. Chỉ giảm được bằng viết nội-dung THẬT có nguồn kiểm-chứng = content-creation campaign riêng, KHÔNG phải grind chuẩn-hoá.

- 2026-07-26 · **R20.8 complexity floor 3 → 14 (KHÔNG phải nợ mới — nợ CŨ vừa lộ ra)**. `agent/gpt55_quality_burst.py` mang UTF-8 BOM ngay trước shebang từ commit gốc của file. `scripts/checks/check_complexity.py:54-55` đọc `read_text(encoding="utf-8", errors="replace")` rồi `ast.parse` trong `except SyntaxError: continue`, nên BOM → U+FEFF → file bị **bỏ qua im lặng** và 11 vi phạm R20.8 CÓ SẴN trong file không bao giờ được đếm. Baseline 3 vì thế chưa bao giờ đúng. Đo trực tiếp để chứng minh: khôi phục BOM → count=3; gỡ BOM → count=14 (cùng một cây mã nguồn). BOM bị gỡ ở tranche pinned-egress vì `tests/test_pinned_http_consumers.py` phải `ast.parse` chính file này (và vì BOM đứng trước shebang khiến file mất khả năng chạy trực tiếp trên Unix). 11 hàm lộ ra đều là code CŨ, không do tranche này tạo: validate_candidate_record 19, relationship_targets 27, build_manifest 17, source_candidate_for_entity 19, candidate_places_for_entity 18, placeid_candidate_from_decision 21, heuristic_quality_status 14, audit_accuracy_chunk 17, audit_relationship_chunk 19, generate_heuristic_eval_cases 24, enforce_apply_policy 14. 3 vi phạm cũ trong `agent/server.py` (chat 117 / chat_stream 115 / _event_stream_body 72) giữ nguyên diện defer đã ghi ở ROADMAP. Nâng floor = ghi nhận SỰ THẬT, KHÔNG phải cấp phép thêm nợ; refactor 11 hàm này là campaign riêng, ngoài phạm vi tranche bảo mật egress. Cùng đợt: `tests/test_notifications.py` vẫn còn BOM → cùng điểm mù, đã mở task follow-up cho `check_test_pairing.py`/`check_complexity.py` đọc `utf-8-sig`.

- 2026-08-05 · **R20.8 baseline 14 → 26 — CHỦ DỰ ÁN QUYẾT (phiên tri-region-color).** Khác hai lần đính chính trước (2026-07-26: thước đo hỏng làm nợ cũ vô hình), lần này là **nợ THẬT, mã MỚI**: 12 hàm đến cùng 13 commit của nhánh itinerary optimizer được gộp vào worktree `tri-region-color` (gần nhất `fb004df3` "integrate joint POI route selection into generator"). Chủ dự án được trình 3 phương án — refactor / nâng baseline kèm giải trình / tách task riêng — và chọn nâng baseline.

  **12 hàm, chia hai nhóm:**
  - *Validate dataclass (4)* — `itinerary_selection.__post_init__` ×3 (15, 17, 13) + `itinerary_schedule.__post_init__` ×2 (23, 21, trong đó một cái thuộc nhóm này). Complexity ở đây gần như toàn bộ là chuỗi kiểm tra tham số đầu vào, mỗi nhánh một `raise` — tách nhỏ chỉ đổi chỗ chứ không giảm số quyết định thật.
  - *Thuật toán (8)* — `select_and_schedule_day` **126**, `_build_joint_day_plans` 24, `_build_day_schedule` 19, `evaluate` 17, `_selection_pool_state` 16, `_candidate_fee_value` 14, `prune_candidates` 14.

  **Nợ chưa trả, KHÔNG được coi là đã xử lý:** `select_and_schedule_day` (`agent/itinerary_selection.py:213`) ở mức **126 — hơn 10 lần ngưỡng 12**, cao hơn cả `server.py::chat` (117) vốn đã được ghi DEFER. Nâng baseline chỉ ngăn nó xấu thêm, không làm nó tốt lên. Trước khi nhánh này merge vào `main`, hàm đó nên có task refactor riêng theo cách đã dùng cho `server.py` đợt 2026-07-10 (test-first, verify GOLDEN byte-identical, extract-verbatim).

  **Ratchet vẫn có răng:** 26 là trần mới; mọi vi phạm PHÁT SINH THÊM vẫn bị chặn. Đã kiểm trước khi nâng rằng không có vi phạm nào thuộc phiên làm việc này — 2 hàm do phiên tạo (`fix_known_data_errors.main`, `audit_content_integrity.audit`) đã tách ở `cf7b84d9`, hàm thứ 3 (`check_test_pairing.run`) tách trong chính commit này.

  **R20.4 coverage KHÔNG nâng** dù `run_hard --all` local báo 1 vi phạm: nội dung vi phạm là *"coverage.json missing or unreadable"*, tức cổng fail-closed vì thiếu artifact, không phải module nào dưới ngưỡng. CI sinh `coverage.json` ở `ci.yml` bước "Run tests with coverage" **trước** bước chạy cổng, nên ở đó R20.4 đo số thật. Nâng baseline lên 1 sẽ cấp phép cho một module thật sự tụt coverage trong CI — đúng kiểu baseline-hoá mà `baseline_tool.py` cấm.

- 2026-08-05 · **Đổi THƯỚC ĐO cho R30.3 / R30.2 / R50.4 — baseline điều chỉnh theo, KHÔNG phải nợ mới.**
  - **R30.3 270 → 307, R30.2 623 → 687.** `RegexCheck` mặc định ghi MỘT violation cho mỗi DÒNG có khớp, không phải mỗi match. Với một ratchet, đó là lỗ: gộp 3 màu cũ vào chung một dòng là mua được 2 suất cho màu cứng MỚI ở chỗ khác mà tổng không đổi (ratchet chỉ nhìn tổng). Bật `count_matches=True` cho cả hai. Chênh lệch 37 và 64 chính là số suất ẩn đã tồn tại sẵn — mã nguồn không đổi một dòng nào. Cùng đợt: thêm `web-nuxt/app.vue` + `error.vue` vào roots (trước đây ngoài tầm quét, đo được +0 vi phạm) và sửa một bug lookahead trong pattern màu — `\(\s*(?!var\()` bị engine backtrack `\s*` về rỗng nên `rgb( var(--x) )` (dùng token, hợp lệ) vẫn bị tính; đếm theo dòng che lỗi này vì cùng dòng thường đã có match khác.
  - **R50.4 soft → soft-ratchet, baseline 245.** `run_hard` chỉ chặn `hard` và các `*-ratchet`, nên rule ở tầng `soft` thuần không bao giờ chặn gì — kể cả khi số entity mỏng tăng vọt. Ratchet ở đây KHÔNG ép trả nợ cũ: mục "Floor legitimate" phía trên đã ghi 245 entity mỏng là irreducible bằng grind (§1.7 cấm độn chữ cho đủ ngưỡng; giảm đúng cách là viết nội dung thật có nguồn). Nó chỉ đảm bảo entity mỏng MỚI không lọt vào im lặng.

- 2026-08-05 · **R20.5b — rule MỚI, hard-ratchet, baseline 275.** R20.5 chỉ đi một chiều (hợp đồng → code): route thêm vào `agent/` mà không ai viết vào `docs/api-contract.md` thì `--all` không thấy. Đo: 367 route đọc được từ `agent/`, **275 chưa được hợp đồng nhắc tới**. Đặt tầng `hard` = 0 là bất khả thi, nên dùng ratchet đúng tinh thần bộ chuẩn — nợ cũ đứng yên, route MỚI bắt buộc có mô tả. Kèm theo, `_decorated_paths` nay đọc được `@router.get(HANG_SO)` (trước chỉ nhận literal, nên route khai bằng hằng số vô hình với cả hai chiều).

- 2026-08-05 · **R30.6 axe baseline 0 → 5 — chủ dự án duyệt thêm scan vào CI.** Cổng có checker từ 2026-07-10 nhưng không nơi nào sinh `axe-report.json`, nên chưa từng chạy. Nguồn sinh nay là `scripts/axe_scan.mjs` (CDP thuần trên Chrome sẵn có của runner — không puppeteer/playwright, không tải browser riêng, giữ §B8), chạy ở job frontend sau `npm run preview`.
  Lần quét đầu tiên trên 14 trang công khai lộ **8 violation serious+**. Đã sửa ngay 5 cái là lỗi markup rõ ràng: 4 × `aria-allowed-attr` (critical) do `aria-pressed` đặt trên `<NuxtLink>` — thuộc tính đó chỉ hợp lệ với `role="button"`, tab điều hướng phải dùng `aria-current="page"` (le-hoi.vue, su-kien.vue); và 1 × `nested-interactive` (serious) do `.season-timeline` khai `role="img"` trong khi bọc 12 `<button>` bấm được → đổi sang `role="group"` (theo-mua.vue).
  **5 còn lại đều là `color-contrast`** (23 node trên `/`, `/du-lich`, `/ocop`, `/theo-mua`, `/lich-trinh`).
  **CẬP NHẬT cùng ngày: đã sửa hết, baseline 5 → 0.** Quét lại 14 trang: 0 violation serious+. Chẩn đoán qua 2 workflow (17 agent, mỗi nhóm 1 điều tra + 2 lăng kính kiểm chứng độc lập) cho thấy **không ca nào là "chọn sai màu chữ"** — tất cả đều là lỗi cấu trúc:
  - `.see-all` (8 node): `<button>` chưa bao giờ được khai `background`/`border` nên render trên `ButtonFace` của Chrome — màu `#6b6b6b` đó **không tồn tại trong repo** (grep = 0 match). Đối chứng: cùng class trên `<NuxtLink>` ở index.vue không dính lỗi. Vá bằng `background: none; border: 0; font-family: inherit; cursor: pointer` — đúng idiom `.nav-group-btn`/`.link-btn` mà `.see-all` bị sót.
  - `.spot-kicker` (1 node): nền M3 `--surface-container-high` (hue 285.9°) lạc giữa ramp Nocturne (hue 181–184°). Gỡ override để rơi về `--card`. Gộp luôn `.event-mini` cùng vết M3 residue, và xoá selector chết `.spot-desc`.
  - `.stl-m` (12 node): `--accent` không có bản `.dark` trong khi `--secondary`/`--tertiary` đảo sáng-tối, nên ở Nocturne mọi nền quý đều sáng. Riêng `q-summer` thì chữ trắng ĐẶC cũng chỉ đạt 2.16:1 — không màu chữ sáng nào cứu được. Tách nhãn khỏi nền quý bằng cặp `--card`/`--ink`, giữ nguyên 5 màu quý làm mã hoá ngữ nghĩa. Agent còn tính ra **3/5 quý cũng fail ở chế độ SÁNG mà axe chưa quét** — bản vá xử lý cả hai chế độ.
  - `.chip.active` (1 node, tệ nhất 1.26:1): tri-region chỉ ép `color: var(--color-on-action)` mà không khai nền, nên `.dark .chip` kéo nền về `--bg-alt` → gần-đen trên gần-đen. Tách khỏi rule gộp, khai đủ bộ ba border/background/color.
  - `.pace-chip.active` (1 node): chữ trắng cứng trên `--secondary` vốn đảo màu theo theme; đổi sang `--color-on-action` cho khớp hợp đồng tri-region.

  **Chống tái phát:** thêm 2 cặp `action-dark-raised` và `brand-dark-surface` vào `web-nuxt/scripts/check-tri-region-contrast.mjs` — điểm mù để lọt cả 5 ca là bảng đó chỉ khoá cặp trên `canvas`, trong khi phần lớn nội dung nằm trên `surface`/`raised`.

- 2026-08-05 · **R20.8 26 → 18: TRẢ nợ cụm itinerary (12 → 4), kèm 3 hàm DEFER có-lý-do.** Chủ dự án duyệt nâng baseline 14 → 26 sáng nay; buổi chiều trả lại 8 trong số đó bằng refactor thật, không phải nới trần. Kế hoạch dựng bằng workflow 7 agent (phân tích + phản biện đối kháng từng cụm), mọi con số đo bằng chính `scripts/checks/check_complexity.py`.

  **Đã tách (8 hàm):** `TravelMatrix.__post_init__` 23→3 · `ScheduleOptions.__post_init__` 21→5 · `SelectionOptions` 17→6 · `SelectionCandidate` 15→7 · `SelectionResult` 13→10 · `prune_candidates` 14→8 · `evaluate` 17→dưới ngưỡng · `_selection_pool_state` 16→7. Rủi ro merge bằng 0: hai file `itinerary_selection.py`/`itinerary_schedule.py` có blob **byte-identical trên cả 4 nhánh codex** (đã verify). Trước mỗi bước đụng code đều có commit test-only làm lưới an toàn (B3): 18 test ghim thông điệp validate + 1 test nhánh deadline vốn mù hoàn toàn + 3 test cho `_ordered_kept_candidates`.

  **DEFER 1 — `select_and_schedule_day` (`agent/itinerary_selection.py`, cx 126, 404 dòng = 64% file).** Lý do CỨNG, không chủ quan: mô phỏng cú tách verbatim tối đa 9 mảnh theo 12 pha đọc được, đo từng mảnh → `_evaluate_subset` 17, `_run_exact_search` 18, `_run_beam_search` 19, `_run_repair` 19, `_attribute_drop_reasons` 13 — **5 mảnh VẪN vi phạm**, cộng 2 mảnh đúng 12 không dư địa. Số vi phạm repo-wide đi từ 26 lên **29**, và `common.py` so ratchet theo **số hàm** chứ không theo điểm, nên **pre-commit sẽ từ chối commit**. Ép hết ≤12 cần ~15–18 helper nhận 6–13 tham số trong vòng lặp nóng — đúng khuôn "helper-soup nuốt-context" đã ký DEFER cho bộ ba pipeline `server.py` ngày 2026-07-10. Hệ quả kèm theo: "hạ 126 → 106" bằng cách nâng 3 closure lên module-level là **tiến bộ bằng 0 trên thước repo** (hàm vẫn là 1 vi phạm) — không được bán như trả nợ chuẩn. Nguồn essential thật: 4 accumulator mutable bắc cầu qua các pha (`cache`, `feasible_with`, `visit_bound_infeasible`, `deadline_reached`), trong đó pha báo-cáo vừa ĐỌC vừa GHI lại `deadline_reached` — không phải luồng một chiều, nên "context object" chỉ đổi chỗ coupling.

  **DEFER 2 — `_build_joint_day_plans` (`agent/itinerary_gen.py`, cx 24).** Điều phối đặt-chỗ ID xuyên nhiều ngày qua `used_content_ids`/`used_anchor_ids`; tách ra phải truyền và trả lại chính hai tập đó ở mỗi mảnh.

  **DEFER 3 — `_candidate_fee_value` (cx 14).** Chuỗi ép kiểu phòng thủ cho dữ liệu phí đến từ nhiều nguồn định dạng khác nhau; giá trị tách thấp, rủi ro đổi thầm quy tắc parse cao. Mở câu hỏi cho chủ dự án.

  **~~CÒN LÀM ĐƯỢC, chưa làm~~ → ĐÃ LÀM cùng ngày, R20.8 18 → 17 — `_build_day_schedule` (cx 19 → 11).** Không phải essential: tách được bằng cách gộp-trùng `_project_placements` dùng chung với `_project_selection_schedule`. Chưa làm vì cần 5 test mở đường trước (39 dòng hiện không test nào phủ) và có một bẫy đã nhận diện: khoá `warnings` được tính từ **nguồn khác nhau** ở hai chỗ gọi — helper mà tự đọc `schedule.warnings` sẽ âm thầm thêm warning lịch vào đường selection. Đã làm sau khi viết đủ 5 test mở đường, trong đó test thứ 5 ghim đúng cái bẫy: `_project_placements` nhận CHÍNH đối tượng schedule (không phải SelectionResult) và `_schedule_core_diagnostics` cố ý KHÔNG có khoá `warnings`, để mỗi chỗ gọi tự ghép warnings từ nguồn của mình.

## SKIP-log (tự động ghi bởi run_hard khi SKIP_CHECKS hợp lệ)
