# fastapi 0.137.0 regression breaks APIRouter.include_router (copies only 1/N routes,
# silently, no error) -> server.app mounts missing routes, route-mounted tests fail on CI.
# 0.136.3 is the highest known-good; cap <0.137 until upstream fixes. (any starlette OK.)
fastapi>=0.115,<0.137
uvicorn>=0.34
openai>=1.82
python-dotenv>=1.1
pydantic-settings>=2.0
httpx>=0.28,<1
httpcore>=1.0.9,<2
requests>=2.32
ddgs>=7.0
cryptography>=42.0
# mcp 2.0.0 bỏ đường dẫn `mcp.server.fastmcp` mà agent/mcp_server.py import,
# nên `mcp>=1.0` để ngỏ khiến CI kéo 2.0 và toàn bộ test itinerary MCP vỡ ở
# bước collect (CI run 31068058171, 2026-08-06). Local đang 1.27.2.
mcp>=1.0,<2
python-telegram-bot>=20
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25

# Phase 0: Database + Auth + Storage
psycopg2-binary>=2.9
redis>=5.0
Pillow>=10.0
boto3>=1.35
python-multipart>=0.0.11

# Dev / Testing
pytest>=8.0
pytest-cov>=6.0
ruff>=0.9

# Wave 4: 2FA (TOTP) — pure-Python, no paid services
pyotp>=2.9
qrcode>=7.4
