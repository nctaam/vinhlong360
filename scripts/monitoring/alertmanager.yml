route:
  receiver: vinhlong360-notifier
  group_by: [alertname]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: vinhlong360-notifier
    webhook_configs:
      - url: ${VL360_ALERT_WEBHOOK_URL}
        send_resolved: true
